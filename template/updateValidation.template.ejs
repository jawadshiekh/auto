const Joi = require("joi");

const <%= entityName %>UpdateSchema = Joi.object({
  query: Joi.object({}),
  params: Joi.object({bookId: Joi.string().pattern(/^[0-9]+$/, 'numbers').required()}),
  body: Joi.object({name: Joi.string().optional(),}).min(1),
});

module.exports = <%= entityName %>UpdateSchema;
