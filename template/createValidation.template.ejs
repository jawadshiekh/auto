const Joi = require("joi");

const <%= entityName %>CreateSchema = Joi.object({
  query: Joi.object({}),
  params: Joi.object({}),
  body: Joi.object({name: Joi.string().required(),}),
});


module.exports = <%= entityName %>CreateSchema;
